---
description: Smoke test of the MCP's main offline pipeline (6 of the 18 core tools) — conventions, library, FB creation, ST validation, XML generation, XML validation.
allowed-tools: mcp__festo-codesys-mcp__plc_lookup, mcp__festo-codesys-mcp__plc_library, mcp__festo-codesys-mcp__plc_validate, mcp__festo-codesys-mcp__validate_plcopen_xml, mcp__festo-codesys-mcp__create_function_block, mcp__festo-codesys-mcp__generate_plcopen_xml, Read, Write, Bash
---

Run an MCP cycle-test exercising the main offline pipeline (6 of the 18 core tools). Do NOT commit anything at the end.

Sequential steps:

1. **Base conventions** — call `plc_lookup` with `action=list_standard` to list Hungarian prefixes + state machine ranges. Summarize in 3 lines.
2. **Library inventory** — call `plc_library` with `action=list` to confirm the 38 FBs are accessible. Report the count per category.
3. **Create a test FB** — call `create_function_block` with:
   - name: `FB_CycleTestCylinder`
   - description: "Pneumatic cylinder with 5s timeout — generated by /cycle-test"
   - inputs: `bExtend BOOL`, `tTimeout TIME`
   - outputs: `bExtended BOOL`, `bDone BOOL`, `bErr BOOL`, `nErrId DINT`
   - write to `tmp-cycle-test/FB_CycleTestCylinder.st`
4. **Validate ST** — `Read` the generated .st file and call `plc_validate` action=naming with its code. Expect zero naming violations.
5. **Generate PLCopen XML** — call `generate_plcopen_xml` on `tmp-cycle-test/` producing `tmp-cycle-test/cycle-test.xml`.
6. **Validate XML** — call `validate_plcopen_xml` on the generated file. Expect all 13 checks to pass.
7. **Cleanup** — `Bash` run `rm -rf tmp-cycle-test` (Linux/Mac) or `Remove-Item -Recurse -Force tmp-cycle-test` (Windows).
8. **Final report** — a markdown table: tool, status, note.

If any step fails, **stop immediately**, show the full error, and suggest a root cause.
